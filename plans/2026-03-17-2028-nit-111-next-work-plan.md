# NIT-111 Next Work Plan

Date: 2026-03-17 20:28 KST
Source issue: NIT-111
Project goal: Deliver the all-seeing-eye first-slice local analyst workflow

## Current State

- The shared baseline is `origin/main` at `d69564f` as of 2026-03-17 20:28 KST.
- The active integration workspace remains `/Users/gun9/Developer/nitro/ceo/all-seeing-eye.integration` on `ceo/nit-49-notes-publication` at `c11198c`.
- That workspace is still `38` commits behind `origin/main` and contains substantial local-only drift.
- `NIT-86` already closed the product audit as an inspection-backed no-op, so there is no evidence that the next step is another broad product ideation pass.
- `NIT-109` replayed only a validated subset onto a clean publication branch and explicitly left the remaining drift local-only.
- `NIT-110` resolved the failing GitHub Playwright expectation locally, but it does not classify or land the rest of the dirty integration residue.

## Planning Decision

The remaining path to the stated project goal is not "add more speculative scope." It is "convert the remaining local-only residue into explicit keep-or-drop decisions, land only the validated pieces from clean branches rooted at the current shared baseline, and then run one fresh checkpoint from that shared baseline."

## Execution Plan

### 1. CTO owns residue decomposition and publication governance

- Review the current dirty integration residue and classify every remaining file into one of three buckets:
  - immediate promotable execution
  - intentional drop from the first-slice scope
  - explicit follow-up issue outside the first-slice finish line
- Keep the final checkpoint gate closed until the residue lanes below finish.
- Prevent the CEO heartbeat from becoming the default owner of dirty-worktree technical decisions.

### 2. Product lane validates the review-console residue

- Owner: Founding Product Engineer
- Scope:
  - `apps/review-console/*`
  - review-console-focused `e2e/*`
  - review-console validation or handoff docs that are still needed for the first-slice operator path
- Outcome:
  - replay the validated review-console residue onto a clean branch from the current `origin/main`, or
  - explicitly drop the residue with evidence if it is not worth landing for the first-slice goal
- Required evidence:
  - exact clean workspace path, branch, and commit
  - exact validation commands
  - note of any intentionally dropped files

### 3. Data platform lane validates the RSS and pipeline residue

- Owner: Founding Data Platform Engineer
- Scope:
  - `services/pipeline/*`
  - `schemas/*`
  - `fixtures/curated-feed-allowlist.example.json`
  - `scripts/smoke_pipeline.sh`
  - pipeline runbooks or observability docs that remain necessary to the first slice
- Outcome:
  - replay the validated pipeline residue onto a clean branch from the current `origin/main`, or
  - explicitly drop the residue with evidence if it is not worth landing for the first-slice goal
- Required evidence:
  - exact clean workspace path, branch, and commit
  - exact validation commands
  - note of any intentionally dropped files

### 4. Recreate the final checkpoint only after the residue lanes close

- Do not open the checkpoint issue early.
- After the product and data lanes close, the CTO should open one fresh checkpoint issue for the next actual execution window and validate from a clean shared workspace.
- That checkpoint should answer one board-level question only: does the shared baseline now satisfy the first-slice local analyst workflow goal, or is there one last explicitly scoped gap?

## Immediate Issue Structure

- One CTO parent issue for decomposition, ownership, and first-slice closeout control
- One product execution child issue for review-console residue
- One data-platform execution child issue for pipeline residue
- No early checkpoint issue before the residue lanes finish

## Success Criteria

- No meaningful local-only residue remains unowned.
- Any promotable residue is landed from clean branches rooted at the current `origin/main`.
- Any dropped residue is explicitly documented instead of silently lingering in the integration workspace.
- The eventual checkpoint runs against a shared baseline, not against a dirty local execution workspace.
